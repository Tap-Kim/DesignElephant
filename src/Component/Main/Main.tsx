import React from 'react';

const Main = () => {
    return (
        <div className="main">
            <button type="button" className="btn btn-danger">danger</button>
        </div>
    );
};

export default Main;